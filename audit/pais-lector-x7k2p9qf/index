<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<script>document.documentElement.classList.add('js')</script>

<!-- PRIVACY: keep audits out of search. noindex is what actually prevents indexing (robots.txt does not). -->
<!-- Belt-and-suspenders: also set the response header  X-Robots-Tag: noindex  at your host if you can. -->
<meta name="robots" content="noindex, nofollow, noarchive">

<title>Growth Audit — País Lector · Beemio</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">

<style>
  :root{
    --bg:#f6f7fb;--card:#fff;--ink:#14152e;--mut:#5f6280;
    --indigo:#4f46e5;--indigo2:#4338ca;--grn:#16a34a;--amber:#d97706;--red:#e23b3b;--line:#e7e8f2;
  }
  *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
  html{scroll-behavior:smooth;scroll-padding-top:118px}
  body{background:var(--bg);color:var(--ink);font-family:'Plus Jakarta Sans',system-ui,-apple-system,sans-serif;
    letter-spacing:-.01em;-webkit-font-smoothing:antialiased}
  a{color:inherit;text-decoration:none}
  ul{list-style:none}

  .wrap{max-width:920px;margin:0 auto;padding:0 30px}
  .ind{color:var(--indigo)}

  /* ===== sticky header (screen only) ===== */
  .topbar{position:sticky;top:0;z-index:60;background:rgba(246,247,251,.92);backdrop-filter:blur(10px);
    border-bottom:1px solid var(--line)}
  .progress{height:3px;width:100%;background:transparent}
  .progress__bar{height:100%;width:0;background:var(--indigo)}
  .topbar__in{max-width:920px;margin:0 auto;padding:11px 30px;display:flex;justify-content:space-between;align-items:center}
  .brand{font-weight:800;font-size:18px}.brand span{color:var(--indigo)}
  .topbar .right{display:flex;align-items:center;gap:14px}
  .private{font-size:12px;font-weight:700;color:var(--indigo);background:#e9e8fd;padding:5px 11px;border-radius:100px}
  .pdf-btn{display:inline-flex;align-items:center;gap:7px;background:var(--indigo);color:#fff;font-weight:700;
    font-size:13.5px;padding:10px 18px;border-radius:10px;border:none;cursor:pointer;font-family:inherit;transition:.15s}
  .pdf-btn:hover{background:var(--indigo2)}
  /* section nav */
  .secnav{border-top:1px solid var(--line)}
  .secnav__in{max-width:920px;margin:0 auto;padding:0 30px;display:flex;gap:6px;overflow-x:auto;scrollbar-width:none}
  .secnav__in::-webkit-scrollbar{display:none}
  .secnav a{flex-shrink:0;font-size:13px;font-weight:600;color:var(--mut);padding:11px 10px;
    border-bottom:2px solid transparent;white-space:nowrap;transition:color .15s,border-color .15s}
  .secnav a:hover{color:var(--ink)}
  .secnav a.active{color:var(--indigo);border-bottom-color:var(--indigo)}

  /* ===== cover ===== */
  .cover{padding:44px 0 20px;scroll-margin-top:118px}
  .eyebrow{display:inline-flex;align-items:center;gap:8px;font-size:12px;font-weight:700;letter-spacing:.04em;
    text-transform:uppercase;color:var(--indigo);background:#e9e8fd;padding:6px 13px;border-radius:100px}
  .cover h1{font-weight:800;font-size:clamp(30px,5vw,46px);line-height:1.05;letter-spacing:-.03em;margin:18px 0 6px}
  .cover .for{font-size:19px;color:var(--mut)}
  .cover .for b{color:var(--ink);font-weight:800}
  .cover .meta{margin-top:14px;font-size:13px;color:var(--mut);display:flex;gap:16px;flex-wrap:wrap}
  .confidential{margin-top:22px;font-size:12.5px;color:var(--mut);border-left:3px solid var(--line);padding-left:12px;line-height:1.5}

  /* ===== scorecard ===== */
  .scorecard{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin:26px 0 8px}
  .score{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:18px 16px;transition:transform .15s,box-shadow .15s}
  .score:hover{transform:translateY(-3px);box-shadow:0 12px 28px rgba(20,21,46,.07)}
  .score .lab{font-size:12px;font-weight:600;color:var(--mut)}
  .score .val{font-weight:800;font-size:26px;letter-spacing:-.02em;margin:8px 0 8px}
  .score .bar{height:7px;border-radius:6px;background:#eef0f6;overflow:hidden}
  .score .bar__fill{height:100%;border-radius:6px;width:0;animation:grow 1.1s cubic-bezier(.2,.7,.2,1) forwards}
  @keyframes grow{to{width:var(--w)}}
  .st-red{color:var(--red)} .st-amber{color:var(--amber)} .st-grn{color:var(--grn)}
  .bg-red{background:var(--red)} .bg-amber{background:var(--amber)} .bg-grn{background:var(--grn)}
  .score .tag{display:inline-block;margin-top:10px;font-size:11px;font-weight:700;padding:3px 9px;border-radius:100px}
  .tag-red{background:#fdeaea;color:var(--red)} .tag-amber{background:#fdf1e3;color:var(--amber)} .tag-grn{background:#e7f6ec;color:var(--grn)}

  /* ===== sections ===== */
  .section{margin:40px 0;break-inside:avoid;scroll-margin-top:118px}
  .section__k{font-size:12px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--indigo);margin-bottom:8px}
  .section h2{font-weight:800;font-size:clamp(22px,3.2vw,30px);letter-spacing:-.025em}
  .section .lead{color:var(--mut);font-size:16px;line-height:1.6;margin-top:12px}
  .card{background:var(--card);border:1px solid var(--line);border-radius:16px;padding:24px;margin-top:16px;break-inside:avoid}

  /* reveal on scroll (progressive enhancement: only hides when JS is on) */
  .js .reveal{opacity:0;transform:translateY(20px);transition:opacity .6s cubic-bezier(.2,.7,.2,1),transform .6s cubic-bezier(.2,.7,.2,1)}
  .js .reveal.in{opacity:1;transform:none}

  /* headline callout */
  .callout{background:linear-gradient(135deg,var(--indigo),#6d4aff);color:#fff;border-radius:18px;padding:28px 26px;margin-top:16px}
  .callout h3{font-weight:800;font-size:19px;margin-bottom:8px}
  .callout p{opacity:.94;font-size:15.5px;line-height:1.6}

  /* table */
  table{width:100%;border-collapse:collapse;font-size:14.5px}
  thead th{text-align:left;padding:12px 14px;font-size:11.5px;text-transform:uppercase;letter-spacing:.05em;
    color:var(--mut);border-bottom:1px solid var(--line);font-weight:700}
  thead th.r,td.r{text-align:right}
  tbody td{padding:13px 14px;border-bottom:1px solid var(--line)}
  tbody tr{transition:background .12s}
  tbody tr:hover{background:#f7f8fd}
  tbody tr:last-child td{border-bottom:none}
  .kw{font-weight:600}
  .pos{font-weight:700}
  .pill{display:inline-block;font-size:12px;font-weight:700;padding:3px 9px;border-radius:100px}

  /* AI engines grid */
  .engines{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-top:16px}
  .engine{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:18px 14px;text-align:center;
    transition:transform .15s,box-shadow .15s}
  .engine:hover{transform:translateY(-3px);box-shadow:0 12px 28px rgba(20,21,46,.08)}
  .engine .name{font-weight:700;font-size:14.5px;margin-bottom:10px}
  .engine .status{font-size:13px;font-weight:700;display:inline-flex;align-items:center;gap:6px}
  .ai-head{display:flex;align-items:center;gap:20px;flex-wrap:wrap;margin-top:16px}
  .gauge{--pct:12;width:150px;height:150px;border-radius:50%;display:grid;place-items:center;position:relative;flex-shrink:0;
    background:conic-gradient(var(--red) calc(var(--pct)*1%), #eef0f6 0)}
  .js .gauge{--pct:0}
  .gauge::after{content:"";position:absolute;width:112px;height:112px;border-radius:50%;background:var(--bg)}
  .gauge .g{position:relative;z-index:2;text-align:center}
  .gauge .g b{font-weight:800;font-size:34px;letter-spacing:-.02em;display:block;line-height:1}
  .gauge .g span{font-size:12px;color:var(--mut)}
  .ai-head .note{flex:1;min-width:220px;font-size:15.5px;line-height:1.6;color:var(--mut)}
  .ai-head .note b{color:var(--ink)}

  /* checklist */
  .check{display:flex;flex-direction:column;gap:2px}
  .check__row{display:grid;grid-template-columns:26px 1fr auto;gap:12px;align-items:center;padding:13px 4px;border-bottom:1px solid var(--line)}
  .check__row:last-child{border-bottom:none}
  .check__ic{width:24px;height:24px;border-radius:50%;display:grid;place-items:center;font-size:13px;font-weight:800}
  .ic-red{background:#fdeaea;color:var(--red)} .ic-amber{background:#fdf1e3;color:var(--amber)} .ic-grn{background:#e7f6ec;color:var(--grn)}
  .check__row .t{font-size:14.5px;font-weight:500}
  .check__row .s{font-size:12.5px;font-weight:700}

  /* prioritized plan */
  .fix{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:20px 22px;margin-top:12px;break-inside:avoid;
    transition:transform .15s,box-shadow .15s,border-color .15s}
  .fix:hover{border-color:#c9c5f7;box-shadow:0 12px 28px rgba(79,70,229,.10)}
  .fix__head{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-bottom:8px}
  .fix__n{width:26px;height:26px;border-radius:8px;background:#e9e8fd;color:var(--indigo);font-weight:800;font-size:14px;display:grid;place-items:center}
  .fix h4{font-weight:700;font-size:17px;letter-spacing:-.01em;flex:1}
  .fix p{color:var(--mut);font-size:14.5px;line-height:1.6}
  .badge{font-size:11px;font-weight:700;padding:3px 9px;border-radius:100px}
  .b-high{background:#fdeaea;color:var(--red)} .b-med{background:#fdf1e3;color:var(--amber)}
  .b-eff{background:#eef0f6;color:var(--mut)}

  /* opportunity */
  .opp{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:16px}
  .opp .box{background:var(--card);border:1px solid var(--line);border-radius:14px;padding:20px}
  .opp .box.now{opacity:.9}
  .opp .box.pot{border-color:#c9c5f7;background:linear-gradient(180deg,#faf9ff,#fff)}
  .opp .lab{font-size:12px;font-weight:700;text-transform:uppercase;letter-spacing:.05em;color:var(--mut)}
  .opp .big{font-weight:800;font-size:30px;letter-spacing:-.02em;margin:8px 0 4px}
  .opp .pot .big{color:var(--indigo)}
  .opp .sub{font-size:13px;color:var(--mut);line-height:1.5}

  /* CTA */
  .cta{background:var(--ink);color:#fff;border-radius:20px;padding:44px 34px;text-align:center;margin:40px 0 30px;break-inside:avoid;scroll-margin-top:118px}
  .cta h2{font-weight:800;font-size:clamp(24px,3.4vw,34px);letter-spacing:-.025em}
  .cta p{max-width:520px;margin:12px auto 8px;color:#b8b4d6;font-size:16px;line-height:1.55}
  .cta .rec{display:inline-block;margin:14px 0 20px;font-size:13.5px;color:#d7d4f5;background:rgba(255,255,255,.08);padding:8px 16px;border-radius:100px}
  .cta .rec b{color:#fff}
  .cta .btn{display:inline-flex;align-items:center;gap:8px;background:var(--indigo);color:#fff;font-weight:700;
    font-size:16px;padding:15px 30px;border-radius:12px;transition:.15s}
  .cta .btn:hover{background:var(--indigo2)}

  /* floating CTA */
  .floating-cta{position:fixed;bottom:22px;right:22px;z-index:55;background:var(--indigo);color:#fff;font-weight:700;
    font-size:14px;padding:13px 22px;border-radius:100px;box-shadow:0 12px 34px rgba(79,70,229,.4);
    opacity:0;transform:translateY(14px);pointer-events:none;transition:opacity .25s,transform .25s,background .15s}
  .floating-cta.show{opacity:1;transform:none;pointer-events:auto}
  .floating-cta:hover{background:var(--indigo2)}

  footer{padding:26px 0 60px;color:var(--mut);font-size:12.5px;text-align:center;line-height:1.6}
  footer b{color:var(--ink)}

  @media(max-width:760px){
    .scorecard{grid-template-columns:1fr 1fr}
    .engines{grid-template-columns:1fr 1fr}
    .opp{grid-template-columns:1fr}
  }
  @media(max-width:440px){.wrap{padding:0 18px}.topbar__in,.secnav__in{padding-left:18px;padding-right:18px}}
  @media (prefers-reduced-motion:reduce){
    html{scroll-behavior:auto}
    .score .bar__fill{animation:none;width:var(--w)}
    .js .reveal{opacity:1;transform:none;transition:none}
  }

  /* ===== PRINT / PDF ===== */
  @media print{
    @page{margin:14mm}
    *{-webkit-print-color-adjust:exact !important;print-color-adjust:exact !important}
    body{background:#fff}
    .topbar,.progress,.secnav,.pdf-btn,.floating-cta,.noprint{display:none !important}
    .js .reveal{opacity:1 !important;transform:none !important}
    .wrap{max-width:100%;padding:0}
    .cover{padding:0 0 10px}
    .section{margin:22px 0}
    .card,.fix,.engine,.score,.cta,.callout,.opp .box,table{break-inside:avoid}
    .cta{background:#14152e !important}
    .page-break{break-before:page}
  }
</style>
</head>
<body>

  <!-- Sticky header: screen only (hidden in the PDF) -->
  <header class="topbar noprint">
    <div class="progress"><div class="progress__bar" id="pbar"></div></div>
    <div class="topbar__in">
      <a class="brand" href="#overview">Beem<span>io</span></a>
      <div class="right">
        <span class="private">Private audit</span>
        <button class="pdf-btn" onclick="window.print()">&#8595; Download PDF</button>
      </div>
    </div>
    <nav class="secnav"><div class="secnav__in">
      <a href="#overview">Overview</a>
      <a href="#search">Search</a>
      <a href="#ai">AI visibility</a>
      <a href="#technical">Technical</a>
      <a href="#plan">Plan</a>
      <a href="#opportunity">Opportunity</a>
    </div></nav>
  </header>

  <main class="wrap">

    <!-- ============ COVER ============ -->
    <section class="cover" id="overview">
      <span class="eyebrow">Growth Audit · Search &amp; AI Visibility</span>
      <h1>Where <span class="ind">País Lector</span><br>stands in search — and in the AI.</h1>
      <p class="for">Prepared for <b>País Lector</b></p>
      <div class="meta">
        <span>Prepared by Beemio</span><span>·</span>
        <span>July 2026</span><span>·</span>
        <span>Snapshot audit</span>
      </div>
      <p class="confidential">Confidential — prepared exclusively for País Lector. Please don't share this link publicly.
      <!-- SAMPLE AUDIT — every figure below is placeholder. Replace with real findings before sending. --></p>
    </section>

    <!-- ============ SCORECARD ============ -->
    <div class="scorecard reveal">
      <div class="score">
        <div class="lab">Google visibility</div>
        <div class="val st-amber">Weak</div>
        <div class="bar"><div class="bar__fill bg-amber" style="--w:34%"></div></div>
        <span class="tag tag-amber">Page 3+ for core terms</span>
      </div>
      <div class="score">
        <div class="lab">AI visibility</div>
        <div class="val st-red">12<span style="font-size:15px;color:var(--mut)">/100</span></div>
        <div class="bar"><div class="bar__fill bg-red" style="--w:12%"></div></div>
        <span class="tag tag-red">Rarely cited</span>
      </div>
      <div class="score">
        <div class="lab">Technical health</div>
        <div class="val st-amber">4 issues</div>
        <div class="bar"><div class="bar__fill bg-amber" style="--w:55%"></div></div>
        <span class="tag tag-amber">Fixable</span>
      </div>
      <div class="score">
        <div class="lab">Content depth</div>
        <div class="val st-red">Thin</div>
        <div class="bar"><div class="bar__fill bg-red" style="--w:22%"></div></div>
        <span class="tag tag-red">Major gaps</span>
      </div>
    </div>

    <!-- ============ HEADLINE ============ -->
    <section class="section reveal">
      <div class="section__k">The short version</div>
      <h2>The demand is there. Right now it's going to competitors.</h2>
      <div class="callout">
        <h3>País Lector is nearly invisible where its audience is searching.</h3>
        <p>For the terms your buyers actually type — and the questions they now ask ChatGPT and Gemini —
        País Lector barely shows up. The good news: the gaps are concrete and fixable, and almost no competitor
        is optimizing for AI answers yet. That's the window.</p>
      </div>
    </section>

    <!-- ============ GOOGLE SEARCH ============ -->
    <section class="section reveal" id="search">
      <div class="section__k">1 · Google search visibility</div>
      <h2>You're ranking below where the clicks happen.</h2>
      <p class="lead">A sample of core terms and where País Lector currently sits. Most sit on page 2–3,
      where almost no one clicks.</p>
      <div class="card" style="padding:6px 8px">
        <table>
          <thead><tr><th>Target term (sample)</th><th class="r">Est. monthly searches</th><th class="r">Current position</th><th class="r">Opportunity</th></tr></thead>
          <tbody>
            <tr><td class="kw">[core category term]</td><td class="r">8,100</td><td class="r pos st-red">#28</td><td class="r"><span class="pill tag-grn">High</span></td></tr>
            <tr><td class="kw">[buyer-intent term]</td><td class="r">3,600</td><td class="r pos st-amber">#14</td><td class="r"><span class="pill tag-grn">High</span></td></tr>
            <tr><td class="kw">[question / how-to term]</td><td class="r">5,400</td><td class="r pos st-red">not ranking</td><td class="r"><span class="pill tag-grn">High</span></td></tr>
            <tr><td class="kw">[branded term]</td><td class="r">1,200</td><td class="r pos st-grn">#2</td><td class="r"><span class="pill tag-amber">Defend</span></td></tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- ============ AI VISIBILITY ============ -->
    <section class="section reveal" id="ai">
      <div class="section__k">2 · AI visibility (AEO / GEO)</div>
      <h2>When people ask the AI, your name doesn't come up.</h2>
      <div class="ai-head">
        <div class="gauge" data-pct="12"><div class="g"><b data-count="12">12</b><span>/ 100</span></div></div>
        <p class="note">We tested <b>12 category questions</b> across the major AI engines.
        País Lector was cited in <b>0 of them</b>. As buyers shift from Google to ChatGPT, Gemini and Perplexity,
        this is quietly becoming the most valuable place to show up — and almost no one in your category is optimizing for it yet.</p>
      </div>
      <div class="engines">
        <div class="engine"><div class="name">ChatGPT</div><div class="status st-red">&#10007; Not cited</div></div>
        <div class="engine"><div class="name">Gemini</div><div class="status st-red">&#10007; Not cited</div></div>
        <div class="engine"><div class="name">Perplexity</div><div class="status st-red">&#10007; Not cited</div></div>
        <div class="engine"><div class="name">Claude</div><div class="status st-red">&#10007; Not cited</div></div>
      </div>
    </section>

    <!-- ============ TECHNICAL ============ -->
    <section class="section reveal" id="technical">
      <div class="section__k">3 · Technical health</div>
      <h2>A few structural issues are capping everything else.</h2>
      <div class="card">
        <div class="check">
          <div class="check__row"><div class="check__ic ic-red">&#10007;</div><div class="t">Structured data (schema) missing on key pages</div><div class="s st-red">Fix</div></div>
          <div class="check__row"><div class="check__ic ic-amber">!</div><div class="t">Core Web Vitals — slow on mobile (LCP)</div><div class="s st-amber">Improve</div></div>
          <div class="check__row"><div class="check__ic ic-red">&#10007;</div><div class="t">Content not structured for AI extraction (no clear answers)</div><div class="s st-red">Fix</div></div>
          <div class="check__row"><div class="check__ic ic-amber">!</div><div class="t">Thin internal linking between related pages</div><div class="s st-amber">Improve</div></div>
          <div class="check__row"><div class="check__ic ic-grn">&#10003;</div><div class="t">Site is crawlable &amp; indexed</div><div class="s st-grn">Good</div></div>
          <div class="check__row"><div class="check__ic ic-grn">&#10003;</div><div class="t">Mobile-friendly layout</div><div class="s st-grn">Good</div></div>
        </div>
      </div>
    </section>

    <!-- ============ PRIORITIZED PLAN ============ -->
    <section class="section reveal page-break" id="plan">
      <div class="section__k">4 · What we'd do first</div>
      <h2>The prioritized plan.</h2>
      <p class="lead">Ordered by impact. This is the sequence we'd run in the first 90 days.</p>

      <div class="fix">
        <div class="fix__head"><span class="fix__n">1</span><h4>Rebuild the core category pages for search + AI</h4>
          <span class="badge b-high">High impact</span><span class="badge b-eff">Medium effort</span></div>
        <p>Restructure your top pages around the exact terms and questions buyers use, with the answer-first structure
        AI engines extract. This is what moves you off page 3 <em>and</em> makes you citable.</p>
      </div>
      <div class="fix">
        <div class="fix__head"><span class="fix__n">2</span><h4>Add structured data + fix the technical foundation</h4>
          <span class="badge b-high">High impact</span><span class="badge b-eff">Low effort</span></div>
        <p>Schema, internal links and mobile speed — the plumbing that lets every page you publish actually perform.</p>
      </div>
      <div class="fix">
        <div class="fix__head"><span class="fix__n">3</span><h4>Launch a bilingual content track on your biggest gaps</h4>
          <span class="badge b-high">High impact</span><span class="badge b-eff">Ongoing</span></div>
        <p>Specialist EN/ES content targeting the high-intent questions where you don't rank at all today.</p>
      </div>
      <div class="fix">
        <div class="fix__head"><span class="fix__n">4</span><h4>Set up AI-visibility tracking</h4>
          <span class="badge b-med">Medium impact</span><span class="badge b-eff">Low effort</span></div>
        <p>Monitor whether ChatGPT, Gemini, Perplexity and Claude start citing you — measured monthly, since AI visibility shifts.</p>
      </div>
    </section>

    <!-- ============ OPPORTUNITY ============ -->
    <section class="section reveal" id="opportunity">
      <div class="section__k">5 · The opportunity</div>
      <h2>Where this could go.</h2>
      <p class="lead">An illustrative picture of the upside if the plan above is executed. A projection, not a guarantee —
      timing depends on your starting point and how competitive the category is.</p>
      <div class="opp">
        <div class="box now">
          <div class="lab">Today</div>
          <div class="big">~1.2k</div>
          <div class="sub">est. monthly organic visits · cited by 0 AI engines</div>
        </div>
        <div class="box pot">
          <div class="lab">Potential (12 mo)</div>
          <div class="big">meaningful growth</div>
          <div class="sub">ranking on page 1 for core terms · cited across major AI engines</div>
        </div>
      </div>
    </section>

    <!-- ============ CTA ============ -->
    <section class="cta reveal">
      <h2>Let's turn this into growth.</h2>
      <p>We built this audit for free because we think the upside for País Lector is real.
      If you'd like, we'll walk you through it and map the first 90 days together.</p>
      <div class="rec">Best fit to start: <b>Engine</b></div>
      <br>
      <a class="btn" href="mailto:hello@beemio.io?subject=Pa%C3%ADs%20Lector%20audit%20call">Book your strategy call &#8594;</a>
    </section>

  </main>

  <a class="floating-cta noprint" href="mailto:hello@beemio.io?subject=Pa%C3%ADs%20Lector%20audit%20call">Book a call &#8594;</a>

  <footer>
    <b>Beemio</b> · SEO &amp; AI visibility for fintech &amp; crypto · Mexico &amp; Argentina<br>
    Confidential — prepared for País Lector · hello@beemio.io
  </footer>

  <script>
  (function(){
    var doc = document;
    var reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var pbar = doc.getElementById('pbar');
    var fcta = doc.querySelector('.floating-cta');

    function onScroll(){
      var el = doc.documentElement;
      var st = el.scrollTop || doc.body.scrollTop;
      var max = el.scrollHeight - el.clientHeight;
      if(pbar) pbar.style.width = (max > 0 ? st/max*100 : 0) + '%';
      if(fcta){ if(st > 520) fcta.classList.add('show'); else fcta.classList.remove('show'); }
    }
    window.addEventListener('scroll', onScroll, {passive:true});
    onScroll();

    function easeOut(t){ return 1 - Math.pow(1-t, 3); }
    function animateCount(el){
      var target = parseFloat(el.dataset.count);
      if(reduce){ el.textContent = target; return; }
      var start = null, dur = 900;
      function step(ts){ if(!start) start = ts; var p = Math.min((ts-start)/dur, 1);
        el.textContent = Math.round(target * easeOut(p));
        if(p < 1) requestAnimationFrame(step); else el.textContent = target; }
      requestAnimationFrame(step);
    }
    function animateGauge(el){
      var target = parseFloat(el.dataset.pct);
      if(reduce){ el.style.setProperty('--pct', target); return; }
      var start = null, dur = 1000;
      function step(ts){ if(!start) start = ts; var p = Math.min((ts-start)/dur, 1);
        el.style.setProperty('--pct', (target*easeOut(p)).toFixed(2));
        if(p < 1) requestAnimationFrame(step); else el.style.setProperty('--pct', target); }
      requestAnimationFrame(step);
    }
    function fire(scope){
      var c = scope.querySelectorAll ? scope.querySelectorAll('[data-count]') : [];
      for(var i=0;i<c.length;i++){ if(!c[i]._d){ c[i]._d=1; animateCount(c[i]); } }
      var g = scope.querySelectorAll ? scope.querySelectorAll('[data-pct]') : [];
      for(var j=0;j<g.length;j++){ if(!g[j]._d){ g[j]._d=1; animateGauge(g[j]); } }
    }

    var reveals = [].slice.call(doc.querySelectorAll('.reveal'));
    // set counters to 0 initially (only when we'll animate)
    if(!reduce && 'IntersectionObserver' in window){
      doc.querySelectorAll('[data-count]').forEach(function(el){ el.textContent = '0'; });
    }
    if('IntersectionObserver' in window && !reduce){
      var io = new IntersectionObserver(function(entries){
        entries.forEach(function(e){ if(e.isIntersecting){ e.target.classList.add('in'); io.unobserve(e.target); fire(e.target); } });
      }, {rootMargin:'0px 0px -8% 0px', threshold:0.08});
      reveals.forEach(function(el){ io.observe(el); });
    } else {
      reveals.forEach(function(el){ el.classList.add('in'); });
      fire(doc);
    }

    // scrollspy for the section nav
    var links = [].slice.call(doc.querySelectorAll('.secnav a'));
    var map = {};
    links.forEach(function(a){ map[a.getAttribute('href').slice(1)] = a; });
    if('IntersectionObserver' in window){
      var spy = new IntersectionObserver(function(entries){
        entries.forEach(function(e){ if(e.isIntersecting){
          links.forEach(function(l){ l.classList.remove('active'); });
          if(map[e.target.id]) map[e.target.id].classList.add('active');
        }});
      }, {rootMargin:'-45% 0px -50% 0px'});
      Object.keys(map).forEach(function(id){ var el = doc.getElementById(id); if(el) spy.observe(el); });
    }

    // make sure the PDF always shows everything, no matter the scroll state
    window.addEventListener('beforeprint', function(){
      reveals.forEach(function(el){ el.classList.add('in'); });
      doc.querySelectorAll('[data-count]').forEach(function(el){ el.textContent = el.dataset.count; });
      doc.querySelectorAll('[data-pct]').forEach(function(el){ el.style.setProperty('--pct', el.dataset.pct); });
    });
  })();
  </script>

</body>
</html>
